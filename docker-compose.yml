services:
  postgres:
    image: postgres:16
    container_name: tg_kb_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tg_kb
    ports:
      - "55433:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
