# Шаблон UAT для Section 5 (Критичные сценарии)

Используй этот файл как рабочий протокол во время ручного Telegram UAT.

## Метаданные прогона

- Дата (UTC): 2026-03-27
- Окружение: `local`
- Оператор:
- Telegram-пользователь (`TELEGRAM_ALLOWED_USER_ID`):
- Ветка/коммит бота:
- Версия БД (ревизия alembic, если применимо):

## Что уже подтверждено до ручного UAT

- [x] `Section 2` закрыт (`SECTION2_ENV_CHECK: PASS`).
- [x] Локальный pre-UAT smoke пройден: `pwsh ./scripts/section5_local_smoke.ps1` -> `SECTION5_LOCAL_SMOKE: PASS`.
- [x] Регрессионные тесты зелёные: `python -m pytest -q` -> `43 passed` (на 2026-03-26).
- [x] Бот запущен в обычном локальном терминале (не в ограниченном runner), polling активен.

## Быстрый порядок выполнения

1. Подними бот в локальном терминале.
2. Выполни кейсы `UAT-01` ... `UAT-07` в порядке ниже.
3. Для каждого кейса заполни `Фактически`, `Статус`, `Доказательства`.
4. Все `PASS` перенеси в [PROD_READINESS_CHECKLIST.md](/d:/Development_codex/tg_db/PROD_READINESS_CHECKLIST.md) (Section 5).

## Сводка статусов (заполнить после прогона)

- UAT-01 `/start` + auth guard: `[x] PASS` / `[ ] FAIL`
- UAT-02 `/add` ручной флоу: `[x] PASS` / `[ ] FAIL`
- UAT-03 `/search` `/list` `/entry` `/status`: `[x] PASS` / `[ ] FAIL`
- UAT-04 Флоу топиков: `[x] PASS` / `[ ] FAIL`
- UAT-05 Import/Export: `[x] PASS` / `[ ] FAIL`
- UAT-06 Коллекции: `[x] PASS` / `[ ] FAIL`
- UAT-07 `/stats`: `[x] PASS` / `[ ] FAIL`

## Журнал выполнения

### UAT-01 `/start` + auth guard

- Шаги:
  - `/start` от разрешенного пользователя.
  - `/start` от неразрешенного пользователя (или контролируемая симуляция).
- Ожидается:
  - разрешенный пользователь получает штатный ответ;
  - неразрешенный пользователь блокируется.
- Фактически:
  - для разрешенного пользователя `/start` возвращает приветствие и список команд;
  - для второго (неразрешенного) пользователя возвращается `Access denied`.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства (ID сообщений, скриншоты, логи):
  - ответы Telegram от 2026-03-27 для разрешенного и неразрешенного аккаунтов

### UAT-02 Ручной флоу `/add`

- Шаги:
  - `/add` в режиме URL.
  - `/add` в режиме заметки.
- Ожидается:
  - оба сценария завершаются успешно и создают записи;
  - dedup/валидация работают корректно.
- Фактически:
  - URL-режим: создана запись `UAT Manual URL`, ID `899c4c42-f311-442a-ae5f-3120f044bf5b`, URL нормализован;
  - Note-режим: создана запись `UAT Manual Note`, ID `db3a893f-842d-405a-99aa-1f01c863e37f`, URL `-`;
  - `/list limit=5` подтверждает наличие обеих новых записей.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - ответы `Saved entry` по двум сценариям `/add` от 2026-03-27
  - вывод `/list limit=5` с обеими записями

### UAT-03 `/search`, `/list`, `/entry`, `/status`

- Шаги:
  - `/search <query>`
  - `/list limit=5`
  - `/entry <uuid>`
  - `/status <uuid> <status>`
- Ожидается:
  - результаты возвращаются и соответствуют состоянию БД;
  - переходы статусов следуют разрешенному workflow.
- Фактически:
  - `/list limit=5` вернул 5 записей с UUID, статусом и топиком;
  - `/entry e4138a7e-5925-462c-bbfa-1b1b8c32f00c` вернул детальную карточку записи;
  - `/status e4138a7e-5925-462c-bbfa-1b1b8c32f00c To Read` и затем `... Verified` отработали успешно;
  - `/search PostgreSQL` вернул релевантный результат `PostgreSQL Backup Basics [New]`.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - ID записи для проверки: `e4138a7e-5925-462c-bbfa-1b1b8c32f00c`
  - Примеры UUID из `/list`: `09b0b2fc-c66d-4065-bf3d-d33933b206e6`, `c5f64cba-c919-4f9c-a270-369e5631d8eb`

### UAT-04 Флоу топиков

- Шаги:
  - `/topics`
  - `/topic_add <name>`
  - `/topic_add "Neural Networks / AI" -> Codex`
  - `/topic_rename`
- Ожидается:
  - иерархия топиков обновляется корректно;
  - созданная подтема визуально отличается от корневой в списке/кнопках;
  - новые/переименованные топики видны в последующих командах.
- Фактически:
  - `/topic_add UAT_Topic_1` отработал успешно (`Topic created`);
  - `/topic_rename <uuid> UAT_Topic_1_Renamed` отработал успешно;
  - `/topics` подтверждает создание и переименование.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - ответы Telegram по `/topic_add`, `/topic_rename`, `/topics` (2026-03-27)

### UAT-05 Import/Export

- Шаги:
  - `/import` с CSV: `docs/uat_samples/uat05_import_sample.csv`
  - `/import` с JSON: `docs/uat_samples/uat05_import_sample.json`
  - `/export csv limit=20`
  - `/export json limit=20`
- Ожидается:
  - import jobs завершаются успешно;
  - экспортируемые файлы создаются и скачиваются;
  - контент в экспорте соответствует импортированным/текущим данным.
- Подсказка по проверке:
  - отправь `/import`, затем прикрепи файл как документ с подписью `/import`;
  - повтори для CSV и JSON;
  - после `/export` проверь, что файлы открываются и содержат ожидаемые записи.
- Фактически:
  - импорт CSV и JSON завершился без ошибок;
  - экспорт CSV и JSON доступен для скачивания;
  - `/export csv limit=20` -> `Export done. Job 586f3753-c104-4e8b-9a92-6166eb3a4c77 records=6`;
  - `/export json limit=20` -> `Export done. Job 44e622d3-e0ad-46e9-a7b6-eebc96bfc418 records=6`.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - Job CSV: `586f3753-c104-4e8b-9a92-6166eb3a4c77`
  - Job JSON: `44e622d3-e0ad-46e9-a7b6-eebc96bfc418`

### UAT-06 Коллекции

- Шаги:
  - `/collection_add`
  - `/collections`
  - `/collection_run`
- Ожидается:
  - коллекция сохраняется и отображается в списке;
  - выполнение коллекции возвращает данные, соответствующие фильтрам.
- Фактически:
  - коллекция `uat_new` сохранена и запускается;
  - `/collection_run` вернул ожидаемый список записей со статусом `New`.
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - вывод `/collection_run` для `uat_new` с 5 записями (2026-03-27)

### UAT-07 `/stats`

- Шаги:
  - `/stats`
- Ожидается:
  - дашборд отображается без ошибок;
  - метрики правдоподобны для текущего набора данных.
- Фактически:
  - `/stats` вернул корректные агрегаты без ошибок;
  - `Total entries: 6`, `Inbox size (New): 5`, `Verified coverage: 0.167`;
  - срезы по статусам и топикам согласуются с данными (`New: 5`, `Verified: 1`, `Useful Channels: 6`).
- Статус: `[x] PASS` / `[ ] FAIL`
- Доказательства:
  - Снимок ответа `/stats` от 2026-03-27

## Дефекты / Follow-ups

- ID:
- Severity:
- Описание:
- Owner:
- Смягчение / влияние на rollback:

## Финальное решение по UAT

- Результат: `[x] PASS` / `[ ] FAIL`
- Подтвердил:
- Дата (UTC):
- Комментарии: Все кейсы `UAT-01 ... UAT-07` пройдены в локальном Telegram UAT.
