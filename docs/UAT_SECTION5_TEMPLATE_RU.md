# Шаблон UAT для Section 5 (Критичные сценарии)

Используй этот файл во время UAT для Section 5.

## Метаданные теста

- Дата (UTC):
- Окружение:
- Оператор:
- Telegram-пользователь:
- Версия/коммит бота:

## Предусловия

- Локальный деплой выполнен.
- `Section 2` закрыт (`SECTION2_ENV_CHECK: PASS` зафиксирован).
- Локальная предварительная проверка выполнена: `pwsh ./scripts/section5_local_smoke.ps1` (должно завершиться `SECTION5_LOCAL_SMOKE: PASS`).
- Тестовый набор данных доступен (топики, записи, примеры файлов для import/export).

## Журнал выполнения

Для каждого кейса фиксируй команду, ожидаемый результат, фактический результат и статус.

### UAT-01 `/start` + auth guard

- Команда(ы):
  - `/start` от разрешенного пользователя
  - `/start` от неразрешенного пользователя (или контролируемая симуляция)
- Ожидается:
  - разрешенный пользователь получает штатный ответ
  - неразрешенный пользователь блокируется
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства (ID сообщений/скриншоты):

### UAT-02 Ручной флоу `/add`

- Команда(ы):
  - `/add` в режиме URL
  - `/add` в режиме заметки
- Ожидается:
  - оба сценария завершаются и создают записи
  - dedup/валидация работают корректно
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

### UAT-03 `/search`, `/list`, `/entry`, `/status`

- Команда(ы):
  - `/search <query>`
  - `/list limit=5`
  - `/entry <uuid>`
  - `/status <uuid> <status>`
- Ожидается:
  - результаты возвращаются и соответствуют состоянию БД
  - переходы статусов следуют разрешенному workflow
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

### UAT-04 Флоу топиков

- Команда(ы):
  - `/topics`
  - `/topic_add`
  - `/topic_rename`
- Ожидается:
  - иерархия топиков обновляется корректно
  - новые/переименованные топики видны в последующих командах
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

### UAT-05 Import/Export

- Команда(ы):
  - `/import` с CSV: `docs/uat_samples/uat05_import_sample.csv`
  - `/import` с JSON: `docs/uat_samples/uat05_import_sample.json`
  - `/export` с фильтрами
- Ожидается:
  - import jobs завершаются успешно
  - экспортируемые файлы создаются и скачиваются
- Быстрый сценарий:
  - отправь в чат с ботом команду `/import`
  - прикрепи `uat05_import_sample.csv` как документ с подписью `/import`
  - прикрепи `uat05_import_sample.json` как документ с подписью `/import`
  - выполни `/export csv limit=20` и `/export json limit=20`
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

### UAT-06 Коллекции

- Команда(ы):
  - `/collection_add`
  - `/collections`
  - `/collection_run`
- Ожидается:
  - коллекция сохраняется и выполняется
  - вывод соответствует фильтрам запроса
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

### UAT-07 `/stats`

- Команда(ы):
  - `/stats`
- Ожидается:
  - дашборд отображается без ошибок
  - метрики правдоподобны для текущего набора данных
- Фактически:
- Статус: `[ ] PASS` / `[ ] FAIL`
- Доказательства:

## Дефекты / Follow-ups

- ID:
- Severity:
- Описание:
- Owner:
- Смягчение / влияние на rollback:

## Финальное решение по UAT

- Результат: `[ ] PASS` / `[ ] FAIL`
- Подтвердил:
- Комментарии:
